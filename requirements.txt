# Medical Diagnosis AI — Dependencies

# Web framework
flask>=3.0
flask-cors>=4.0

# Database
pymongo>=4.6

# Web scraping
requests>=2.31
beautifulsoup4>=4.12

# NLP / Text processing
nltk>=3.8
scikit-learn>=1.3

# Deep learning — LSTM
tensorflow>=2.15

# Deep learning — Transformer (BioBERT)
torch>=2.1
transformers>=4.36
sentence-transformers>=2.2

# Data handling
numpy>=1.24
pandas>=2.0
joblib>=1.3
tqdm>=4.65

# SciSpacy (UMLS linking) — optional
# pip install scispacy
# pip install https://s3-us-west-2.amazonaws.com/ai2-s2-scispacy/releases/v0.5.4/en_core_sci_lg-0.5.4.tar.gz

# GridFS (included in pymongo)
# gridfs is part of pymongo, no extra install needed
